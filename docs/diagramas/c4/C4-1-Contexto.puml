@startuml C4-1-Contexto
!include <C4/C4_Context>

title ms-contratos — C4 Nivel 1: Contexto

LAYOUT_WITH_LEGEND()

Person(actor, "Arrendatario", "Busca, visita y firma contratos")
System(sys, "ms-contratos", "Contratos del caso caso11: mantiente el seguimiento en tiempo real del proceso: consume los eventos y señales del dominio (estados, posición, sensores, etapas), mantiene el estado vigente consultable en milisegundos y notifica las transiciones")

System_Ext(notif, "ms-mensajeria", "Mensajería del caso")
System_Ext(cognito, "Amazon Cognito", "Autenticación y JWT por rol")
System_Ext(bus, "Amazon EventBridge", "Bus de eventos de dominio")

Rel(actor, sys, "Usa el servicio", "HTTPS / REST + JWT")
Rel_Back(notif, sys, "Mensajería del caso", "EventBridge / SQS")
Rel_Back(cognito, sys, "Autenticación y JWT por rol", "JWT")
Rel(sys, bus, "Bus de eventos de dominio", "AWS SDK")

SHOW_LEGEND()
@enduml
